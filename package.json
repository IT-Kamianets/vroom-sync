{
  "name": "com.itkamianets.vroom-sync",
  "version": "0.1.0",
  "displayName": "VRoom Sync",
  "description": "Stores and synchronizes VRoom scenes (3d-scene-schema JSON) with Firebase Firestore. Requires the Firebase Unity SDK's Firestore + App packages (com.google.firebase.firestore, com.google.firebase.app) to be installed in the consuming project; compiles to a no-op without them.",
  "unity": "6000.6",
  "license": "MIT",
  "author": {
    "name": "IT Kamianets"
  },
  "keywords": [
    "vroom",
    "firebase"
  ],
  "dependencies": {
    "com.itkamianets.3d-unity-scene": "0.1.0"
  }
}
